receivers:
  otlp:
    endpoint: 0.0.0.0:55680
  zipkin:
    endpoint: 0.0.0.0:9411

exporters:
  logging:

  elastic:
    apm_server_url: "https://d68cdc374.apm.us-central1.gcp.cloud.es.io"
    secret_token: "PASS"

processors:
  batch:
  queued_retry:

extensions:
  health_check:
  pprof:
    endpoint: :1888
  zpages:
    endpoint: :55679

service:
  extensions: [pprof, zpages, health_check]
  pipelines:
    traces:
      receivers: [otlp, zipkin]
      exporters: [elastic, logging]
      processors: [batch, queued_retry]
